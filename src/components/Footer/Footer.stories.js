import React from 'react'
import Footer from './Footer'

export default {
    title: 'footer',
    component: Footer,
}

const Template = (props) => <Footer />

export const Default = Template.bind({})
